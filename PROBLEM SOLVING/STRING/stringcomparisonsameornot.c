#include<stdio.h>
#include<string.h>
int main(){
	char name[20];
	char str[20];
	gets(name);
	gets(str);
	int i=0,x=0;
	
	while(name[i]!='\0'){
		i++;
	}
	while(str[x]!='\0'){
		x++;
	}

	if(i==x){
		while(str[j]!='\0'{
		if(name[i]==name[j]){
			
		}
		else{
			
			break;
			
		}
	}
	}

	if(flag==0){
		printf("PAlindrome\n");
	}
	else{
		printf("Not a PAlindrome\n");
	}
	puts(name);
	return 0;
}